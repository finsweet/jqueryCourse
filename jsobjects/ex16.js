// Replaces all occurrences of “-” in "categories-animals" with a colon “:” .
let myString = "categories-animals";
let replaced_string = myString.replace("-", ":");
$("#resultdiv").text(replaced_string);
